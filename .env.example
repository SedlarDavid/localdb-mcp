# localdb-mcp — environment variables
# Copy to .env and fill in real values, or export in your shell.
# Never commit .env or paste real URIs into chat.

# PostgreSQL (connection id: postgres)
# Example: postgres://user:password@localhost:5432/mydb?sslmode=disable
# Docker:   postgres://postgres:postgres@localhost:5432/postgres?sslmode=disable
MCP_DB_POSTGRES_URI=

# SQL Server (connection id: sqlserver)
# Example: sqlserver://user:password@localhost:1433?database=mydb
# Docker:   sqlserver://sa:YourStrong!Passw0rd@localhost:1433?database=master
MCP_DB_SQLSERVER_URI=

# SQLite (connection id: sqlite)
# Path to a SQLite database file. Use ":memory:" for in-memory.
# Example: /path/to/mydb.sqlite
# Example: file:mydb.sqlite?mode=rwc
MCP_DB_SQLITE_URI=
